server:
  port: 8765

spring:
  application:
    name: d-service

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE}
management:
  metrics:
    enable:
      system: false
    binders:
      processor:
        enabled: false
